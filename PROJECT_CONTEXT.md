ПРОЕКТ: Менеджер Яндекс.Диска (Yandex.Disk Manager)
📋 ОПИСАНИЕ ПРОЕКТА
Десктопное приложение с графическим интерфейсом для управления файлами на Яндекс.Диске в рамках небольшой строительной компании. Приложение позволяет отслеживать изменения файлов (в том числе сделанные напрямую через диск), назначать теги, работать с ролями пользователей.
Цель разработки
Выпускная квалификационная работа (ВКР) для университета + создание реального рабочего инструмента.
Ключевые особенности
•	Отслеживание изменений, сделанных напрямую через Яндекс.Диск (вне приложения)
•	Система тегов с сохранением при перемещении/переименовании файлов
•	Ролевая модель (Admin, Manager, Viewer)
•	Лента последних изменений с индикацией источника (через приложение / напрямую)

## 🚀 ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

**Дата:** 16.03.2025
**Этап:** 1 (Модели данных) - ЗАВЕРШЁН
**Следующий этап:** 2 (Ядро работы с Яндекс.Диском) - ОЖИДАЕТ НАЧАЛА

**Что сделано:**
- Создан репозиторий на GitHub
- Настроено виртуальное окружение с Django
- Установлены зависимости
- Создан и настроен Django проект
- Зарегистрировано приложение на Яндексе (получены ключи OAuth)
- **Созданы модели данных: User, Tag, File, ChangeLog**
- **Настроена админка Django**
- **Создан суперпользователь**

**Следующие шаги:**
- Разработка ядра для работы с Яндекс.Диском (авторизация, операции с файлами)
________________________________________
🛠 ТЕХНОЛОГИЧЕСКИЙ СТЕК
Компонент	Технология	Обоснование
Язык	Python 3.10+	Универсальность, простота, библиотеки
Backend	Django + Django REST Framework	Знакомство разработчика, ORM, админка
База данных	SQLite (встроенная)	Не требует отдельного сервера, достаточно для десктопного приложения
GUI	Tkinter	Встроен в Python, простой, достаточный функционал
API Яндекс.Диска	yadisk (библиотека)	Удобная обертка над REST API
Планировщик	schedule + threading	Периодическая проверка изменений
Версионирование	Git + GitHub	Контроль версий, хранение кода
IDE	VS Code	Привычная среда разработчика
________________________________________
🗂 СТРУКТУРА ПРОЕКТА (ПЛАНИРУЕМАЯ)
text
yandex-disk-manager/
├── .gitignore
├── README.md
├── PROJECT_CONTEXT.md (этот файл)
├── requirements.txt
├── manage.py
├── config/
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── core/                          # Основное приложение Django
│   ├── __init__.py
│   ├── admin.py
│   ├── models.py                   # Модели данных
│   ├── views.py                     # API для GUI (если потребуется)
│   ├── urls.py
│   └── yandex/
│       ├── __init__.py
│       ├── auth.py                   # Авторизация в Яндекс.Диске
│       ├── client.py                  # Клиент для API
│       └── sync.py                     # Синхронизация и мониторинг
├── gui/
│   ├── __init__.py
│   ├── main_window.py              # Главное окно
│   ├── auth_dialog.py               # Диалог авторизации
│   ├── widgets/
│   │   ├── file_list.py               # Список файлов
│   │   ├── tag_panel.py               # Панель тегов
│   │   └── notifications.py            # Панель уведомлений
│   └── app.py                          # Запуск GUI
└── docs/                              # Документация (для ВКР)
    ├── user_guide.md
    └── architecture.md
________________________________________
🎯 ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ
Роли пользователей
Роль	Права
Admin	Полный доступ: просмотр, загрузка, скачивание, удаление, управление тегами, управление пользователями
Manager	Просмотр, загрузка, скачивание, назначение тегов
Viewer	Только просмотр и скачивание
Основные возможности
1.	Авторизация в Яндекс.Диске через OAuth (получение и сохранение токена)
2.	Просмотр файлов и папок с диска (как в проводнике)
3.	Работа с файлами:
o	Скачивание на локальный компьютер
o	Загрузка с локального компьютера
o	Удаление (для Admin)
o	Создание папок
4.	Система тегов:
o	Создание цветных тегов
o	Назначение тегов файлам (множественный выбор)
o	Фильтрация файлов по тегам
o	Сохранение тегов при перемещении/переименовании файлов
5.	Мониторинг изменений:
o	Автоматическая проверка каждые 5-15 минут
o	Обнаружение изменений, сделанных напрямую через диск
o	Лента последних изменений с указанием источника
6.	Уведомления:
o	Цветовая индикация в ленте (зеленый - через приложение, оранжевый - напрямую)
o	Всплывающие уведомления о важных изменениях (опционально)
________________________________________
📊 МОДЕЛИ ДАННЫХ (ПРОЕКТИРУЕМЫЕ)
python
# core/models.py (концепция, код будет позже)

class User(AbstractUser):
    """Расширенная модель пользователя"""
    ROLE_CHOICES = [
        ('admin', 'Administrator'),
        ('manager', 'Manager'),
        ('viewer', 'Viewer'),
    ]
    role = models.CharField(max_length=20, choices=ROLE_CHOICES, default='viewer')
    yandex_token = models.TextField(blank=True)  # Зашифрованный токен

class Tag(models.Model):
    """Тег для маркировки файлов"""
    name = models.CharField(max_length=100, unique=True)
    color = models.CharField(max_length=7, default='#3498db')  # HEX
    created_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
    created_at = models.DateTimeField(auto_now_add=True)

class File(models.Model):
    """Информация о файле на Яндекс.Диске"""
    yandex_id = models.CharField(max_length=255, unique=True)  # Ключ для связи
    name = models.CharField(max_length=255)
    path = models.TextField()  # Полный путь на диске
    type = models.CharField(max_length=50)  # file/dir
    mime_type = models.CharField(max_length=100, blank=True)
    size = models.BigIntegerField(default=0)
    created_at = models.DateTimeField()
    modified_at = models.DateTimeField()
    tags = models.ManyToManyField(Tag, related_name='files', blank=True)
    
    # Для отслеживания изменений
    last_synced = models.DateTimeField(auto_now=True)

class ChangeLog(models.Model):
    """Лог изменений файлов"""
    CHANGE_TYPES = [
        ('created', 'Создан'),
        ('modified', 'Изменен'),
        ('moved', 'Перемещен'),
        ('deleted', 'Удален'),
    ]
    SOURCE_TYPES = [
        ('app', 'Через приложение'),
        ('direct', 'Напрямую через диск'),
        ('unknown', 'Неизвестно'),
    ]
    
    file = models.ForeignKey(File, on_delete=models.CASCADE, null=True, blank=True)
    file_path = models.TextField()  # Путь на момент изменения
    change_type = models.CharField(max_length=20, choices=CHANGE_TYPES)
    source = models.CharField(max_length=20, choices=SOURCE_TYPES, default='app')
    changed_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True, blank=True)
    changed_at = models.DateTimeField()
    detected_at = models.DateTimeField(auto_now_add=True)
    old_path = models.TextField(blank=True)  # Для перемещений
________________________________________
🔄 ПЛАН РАБОТ ПО ЭТАПАМ
✅ Этап 0: Подготовка и инфраструктура (1-2 дня)
**Статус: ЗАВЕРШЁН**
Задачи:
•	Обсуждение требований и архитектуры
•	Создание файла PROJECT_CONTEXT.md
•	Создание репозитория на GitHub
•	Настройка .gitignore
•	Создание виртуального окружения
•	Установка базовых зависимостей
•	Инициализация Django проекта
•	Регистрация приложения на Яндексе (получение Client ID/Secret)
•	Проверка работоспособности
Результат: Готовый репозиторий, настроенное окружение, ключи API
________________________________________
⏳ Этап 1: Модели данных и база данных (2-3 дня)
**Статус: ЗАВЕРШЁН**
Задачи:
•	Реализация моделей Django
•	Создание и применение миграций
•	Настройка админки
•	Создание суперпользователя
•	Базовые тесты моделей
________________________________________
⏳ Этап 2: Ядро работы с Яндекс.Диском (3-4 дня)
Статус: ОЖИДАЕТ
Задачи:
•	Модуль авторизации (OAuth)
•	Диалог в Tkinter для ввода кода
•	Сохранение токена в БД
•	Базовые операции: список, скачивание, загрузка
•	Модуль синхронизации
•	Обработка ошибок
________________________________________
⏳ Этап 3: Фоновый мониторинг изменений (2-3 дня)
Статус: ОЖИДАЕТ
Задачи:
•	Планировщик задач
•	Механизм обнаружения изменений
•	Определение источника изменений
•	Сохранение в ChangeLog
•	Фоновый поток
________________________________________
⏳ Этап 4: Графический интерфейс - Основные окна (4-5 дней)
Статус: ОЖИДАЕТ
Задачи:
•	Главное окно (меню, статус-бар)
•	Левая панель (навигация и теги)
•	Центральная панель (список файлов)
•	Правая панель (лента изменений)
•	Диалоги (авторизация, теги, загрузка)
________________________________________
⏳ Этап 5: Интеграция и связывание компонентов (3-4 дня)
Статус: ОЖИДАЕТ
Задачи:
•	Связь GUI с ядром
•	Интеграция системы тегов
•	Подключение мониторинга к GUI
•	Реализация ролевой модели
________________________________________
⏳ Этап 6: Тестирование и отладка (2-3 дня)
Статус: ОЖИДАЕТ
Задачи:
•	Функциональное тестирование
•	Тестирование мониторинга
•	Тестирование тегов
•	Обработка ошибок
•	Оптимизация
________________________________________
⏳ Этап 7: Документация и подготовка к сдаче (2 дня)
Статус: ОЖИДАЕТ
Задачи:
•	Пользовательская документация
•	Техническая документация
•	Подготовка демонстрации
•	Финальный коммит в GitHub
________________________________________
✨ Этап 8 (Опционально): Дополнительные функции
Статус: ОЖИДАЕТ
Возможные улучшения:
•	Поиск файлов
•	Предпросмотр
•	Экспорт отчетов
•	Горячие клавиши
•	Темы оформления
________________________________________
📝 ПРИНЯТЫЕ РЕШЕНИЯ
Дата	Решение	Обоснование
13.03.2025	Используем Python + Django + Tkinter	Знакомство разработчика с Django, простота Tkinter
13.03.2025	Теги хранятся в локальной БД и привязаны к yandex_id	Обеспечивает сохранность тегов при любых изменениях файлов
13.03.2025	Мониторинг через периодический опрос (polling)	API Яндекса не поддерживает webhooks
13.03.2025	Цветовая индикация в ленте изменений	Визуальное разделение источников изменений
________________________________________
🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ И ОГРАНИЧЕНИЯ
Пока не выявлены
